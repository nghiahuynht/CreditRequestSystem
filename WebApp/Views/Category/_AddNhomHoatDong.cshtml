
@model DAL.Models.Category.CategoryActiveGroupViewModel
<div class="modal-content">
    <div class="modal-header p-2">
        @if (Model.Id == 0)
        {
            <h5 class="modal-title">Thêm nhóm hoạt động</h5>
        }
        else
        {
            <h5 class="modal-title">Chỉnh sửa nhóm hoạt động</h5>
        }

    </div>
    <div class="content-body">
        <div class="modal-body p-3">
            <form id="frmCreateActiveGroup">
                <input id="a_active_group_id" hidden value="@Model.Id" />
                <fieldset class="border rounded-1 pb-3 pe-3 ps-3 mb-3">
                    <legend class="float-none w-auto px-3 text-uppercase fw-bold fs-3 mb-0 text-blue">Thông tin nhóm hoạt động</legend>

                    <div class="row">
                        <div class="col-md-6">
                            <label class="form-label">Mã nhóm: <span class="text-red">*</span></label>
                            <input type="text" class="form-control" id="a_code" name="code" value="@Model.Code" />
                        </div>
                        <div class="col-md-6 ">
                            <label class="form-label col-auto">Tên nhóm: <span class="text-red">*</span></label>
                            <input type="text" class="form-control" id="a_name" name="name" value="@Model.Name" />
                        </div>
                        

                    </div>
                    <div class="row">
                        <div class="col-md-6">
                            <label class="form-label">Ghi chú:</label>
                            <textarea class="form-control" id="a_ghi_chu" name="ghi_chu">@Model.Notes</textarea>
                        </div>
                       


                    </div>
                </fieldset>

            </form>
        </div>
    </div>
    <div class="modal-footer p-0">
        <button type="button" class="btn btn-primary btn-footer" id="btnLuuTaoDot" onclick="CreateActiveGroup();"><i class="fas fa-save me-1"></i>&nbsp;Lưu</button>
        <button type="button" class="btn btn-secondary btn-footer" onclick="closeModal_ActiveGroup();" data-bs-dismiss="modal"><i class="fas fa-times me-1"></i>&nbsp;Đóng</button>
    </div>
</div>